apiVersion: v1
kind: Service
metadata:
  name: genesis-backend
  namespace: genesis-ai
  labels:
    app: genesis-backend
spec:
  selector:
    app: genesis-backend
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
  type: ClusterIP